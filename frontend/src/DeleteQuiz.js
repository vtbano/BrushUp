import React from "react";

const DeleteQuiz = ({ title, setQuizzes }) => {
  return (
    <React.Fragment>
      <button className="Delete">Delete</button>
    </React.Fragment>
  );
};

export default DeleteQuiz;
